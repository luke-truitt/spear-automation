@model IEnumerable<SpearAutomation.Models.MOL.Personnel>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DateReturning)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CertificationLevel)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateReturning)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CertificationLevel)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MarineId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MarineId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MarineId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
